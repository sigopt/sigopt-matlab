"""Only uncomment the lines below if using a virtual environment
or if there is ambiguity to the python interpreter's site-packages, 
specifically where the `sigopt-python` package has been installed.

In a typical setting, `pip install sigopt` needs no configuration.
"""

#import sys
#sys.path.append('/path/to/site-packages')
